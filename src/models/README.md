# Models

This directory contains implementations of all predictive models evaluated in the benchmark study, including gradient-boosted tree models, stacked ensemble models, and deep learning models based on CT imaging.
